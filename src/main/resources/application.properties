spring.application.name=petpick

spring.datasource.url=jdbc:mysql://${DB_MASTER_URL}/test_pickpet
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.username=${MASTER_USERNAME}
spring.datasource.password=${MASTER_SECRET}

spring.jpa.generate-ddl=true
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Google OAuth2 Client
#spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID}
#spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET}
#spring.security.oauth2.client.registration.google.scope=profile, email
#spring.security.oauth2.client.registration.google.redirect-uri=${BACKEND_DEVELOP_URL}/login/oauth2/code/google

# JWT
#jwt.jwt-key=${JWT_SECRET_KEY}

# Expiration
#jwt.access-token-expiration=3600000
#jwt.refresh-token-expiration=1209600000
#cookie.cookie-max-age=1209600000